# ─── Database (Supabase PostgreSQL) ──────────────────────
# Add ?connection_limit=10&pool_timeout=30 for production connection pooling
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres"
DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres"

# ─── JWT Authentication ─────────────────────────────────
# Minimum 16 characters. Use a strong random string.
JWT_SECRET="your-jwt-secret-change-this-min-16-chars"
JWT_REFRESH_SECRET="your-refresh-secret-change-this-min-16-chars"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# ─── Midtrans Payment Gateway ───────────────────────────
MIDTRANS_SERVER_KEY="SB-Mid-server-xxx"
MIDTRANS_CLIENT_KEY="SB-Mid-client-xxx"
MIDTRANS_IS_PRODUCTION="false"

# ─── Supabase (Storage + Realtime) ──────────────────────
SUPABASE_URL="https://[PROJECT_REF].supabase.co"
SUPABASE_SERVICE_KEY="your-supabase-service-role-key"

# ─── Server ─────────────────────────────────────────────
PORT=4000
NODE_ENV="development"

# ─── CORS ────────────────────────────────────────────────
# Comma-separated list of allowed origins (preferred over FRONTEND_URL for CORS)
# CORS_ORIGINS="http://localhost:3000,https://your-app.vercel.app"
FRONTEND_URL="http://localhost:3000"

# ─── Platform ───────────────────────────────────────────
# Fee percentage charged on each escrow transaction (0-100)
PLATFORM_FEE_PERCENT=10

# ─── Database Seed ──────────────────────────────────────
# Password for the admin user created during `prisma:seed`. Omit to skip admin seeding.
# ADMIN_SEED_PASSWORD="change-this-admin-password"
